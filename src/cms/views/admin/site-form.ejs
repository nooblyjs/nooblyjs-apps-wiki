<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= isEdit ? 'Edit' : 'Create' %> Site - CMS</title>
    <link rel="stylesheet" href="/applications/cms/css/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <nav class="sidebar">
            <div class="sidebar-header">
                <h2><i class="fas fa-cube"></i> CMS</h2>
            </div>

            <ul class="sidebar-menu">
                <li>
                    <a href="/applications/cms/dashboard">
                        <i class="fas fa-tachometer-alt"></i>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li class="active">
                    <a href="/applications/cms/sites">
                        <i class="fas fa-globe"></i>
                        <span>Sites</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/pages">
                        <i class="fas fa-file-alt"></i>
                        <span>Pages</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/assets">
                        <i class="fas fa-images"></i>
                        <span>Assets</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/templates">
                        <i class="fas fa-layer-group"></i>
                        <span>Templates</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/themes">
                        <i class="fas fa-palette"></i>
                        <span>Themes</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/analytics">
                        <i class="fas fa-chart-bar"></i>
                        <span>Analytics</span>
                    </a>
                </li>
                <li>
                    <a href="/applications/cms/settings">
                        <i class="fas fa-cog"></i>
                        <span>Settings</span>
                    </a>
                </li>
            </ul>

            <div class="sidebar-footer">
                <div class="user-info">
                    <i class="fas fa-user-circle"></i>
                    <span id="current-user"><%= user.username %></span>
                </div>
                <button class="logout-btn" onclick="logout()">
                    <i class="fas fa-sign-out-alt"></i>
                    Logout
                </button>
            </div>
        </nav>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Header -->
            <header class="content-header">
                <div class="header-left">
                    <h1><%= isEdit ? 'Edit' : 'Create New' %> Site</h1>
                    <p><%= isEdit ? 'Update your website settings' : 'Set up your new website' %></p>
                </div>
                <div class="header-right">
                    <button class="btn btn-outline" onclick="window.location.href='/applications/cms/sites'">
                        <i class="fas fa-arrow-left"></i>
                        Back to Sites
                    </button>
                </div>
            </header>

            <!-- Site Form -->
            <div class="form-container">
                <form id="site-form" class="site-form">
                    <input type="hidden" id="site-id" value="<%= site ? site.id : '' %>">

                    <!-- Basic Information -->
                    <div class="form-section">
                        <h2>Basic Information</h2>

                        <div class="form-row">
                            <div class="form-group">
                                <label for="site-name">Site Name *</label>
                                <input type="text" id="site-name" name="name" required
                                       value="<%= site ? site.name : '' %>"
                                       placeholder="My Awesome Website">
                                <small>The internal name for your site</small>
                            </div>

                            <div class="form-group">
                                <label for="site-title">Site Title *</label>
                                <input type="text" id="site-title" name="title" required
                                       value="<%= site ? site.settings.title : '' %>"
                                       placeholder="Welcome to My Website">
                                <small>Displayed in browser title and search results</small>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="site-description">Description</label>
                            <textarea id="site-description" name="description" rows="3"
                                      placeholder="A brief description of your website"><%= site ? site.settings.description : '' %></textarea>
                            <small>Used for SEO and social media previews</small>
                        </div>
                    </div>

                    <!-- Domain Settings -->
                    <div class="form-section">
                        <h2>Domain Settings</h2>

                        <div class="form-row">
                            <div class="form-group">
                                <label for="site-domain">Custom Domain</label>
                                <input type="text" id="site-domain" name="domain"
                                       value="<%= site ? site.domain : '' %>"
                                       placeholder="www.example.com">
                                <small>Optional: Connect your own domain</small>
                            </div>

                            <div class="form-group">
                                <label for="site-subdomain">Subdomain</label>
                                <input type="text" id="site-subdomain" name="subdomain"
                                       value="<%= site ? site.subdomain : '' %>"
                                       placeholder="mysite.cms.com">
                                <small>Your free CMS subdomain</small>
                            </div>
                        </div>
                    </div>

                    <!-- Design Settings -->
                    <div class="form-section">
                        <h2>Design & Theme</h2>

                        <div class="form-group">
                            <label for="site-theme">Theme</label>
                            <select id="site-theme" name="theme">
                                <option value="default" <%= site && site.theme === 'default' ? 'selected' : '' %>>Default</option>
                                <option value="portfolio" <%= site && site.theme === 'portfolio' ? 'selected' : '' %>>Portfolio</option>
                                <option value="business" <%= site && site.theme === 'business' ? 'selected' : '' %>>Business</option>
                                <option value="blog" <%= site && site.theme === 'blog' ? 'selected' : '' %>>Blog</option>
                            </select>
                            <small>Choose a theme for your website</small>
                        </div>
                    </div>

                    <!-- SEO Settings -->
                    <div class="form-section">
                        <h2>SEO Settings</h2>

                        <div class="form-group">
                            <label for="meta-title">Meta Title</label>
                            <input type="text" id="meta-title" name="metaTitle"
                                   value="<%= site ? (site.settings.seo ? site.settings.seo.metaTitle : '') : '' %>"
                                   placeholder="Professional Website | My Business">
                            <small>Title shown in search engine results</small>
                        </div>

                        <div class="form-group">
                            <label for="meta-description">Meta Description</label>
                            <textarea id="meta-description" name="metaDescription" rows="2"
                                      placeholder="A compelling description for search engines"><%= site ? (site.settings.seo ? site.settings.seo.metaDescription : '') : '' %></textarea>
                            <small>Description shown in search engine results</small>
                        </div>

                        <div class="form-group">
                            <label for="meta-keywords">Keywords</label>
                            <input type="text" id="meta-keywords" name="keywords"
                                   value="<%= site ? (site.settings.seo ? site.settings.seo.keywords.join(', ') : '') : '' %>"
                                   placeholder="website, business, professional">
                            <small>Comma-separated keywords for SEO</small>
                        </div>
                    </div>

                    <!-- Form Actions -->
                    <div class="form-actions">
                        <button type="button" class="btn btn-outline" onclick="window.location.href='/applications/cms/sites'">
                            Cancel
                        </button>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i>
                            <%= isEdit ? 'Update Site' : 'Create Site' %>
                        </button>
                    </div>
                </form>
            </div>
        </main>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('site-form');
            form.addEventListener('submit', handleSubmit);
        });

        async function handleSubmit(e) {
            e.preventDefault();

            const formData = new FormData(e.target);
            const siteId = document.getElementById('site-id').value;
            const isEdit = !!siteId;

            // Build site data object
            const siteData = {
                name: formData.get('name'),
                domain: formData.get('domain'),
                subdomain: formData.get('subdomain'),
                theme: formData.get('theme'),
                settings: {
                    title: formData.get('title'),
                    description: formData.get('description'),
                    seo: {
                        metaTitle: formData.get('metaTitle'),
                        metaDescription: formData.get('metaDescription'),
                        keywords: formData.get('keywords').split(',').map(k => k.trim()).filter(k => k)
                    }
                }
            };

            try {
                const url = isEdit ? `/applications/cms/api/sites/${siteId}` : '/applications/cms/api/sites';
                const method = isEdit ? 'PUT' : 'POST';

                showNotification(isEdit ? 'Updating site...' : 'Creating site...', 'info');

                const response = await fetch(url, {
                    method: method,
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(siteData)
                });

                if (response.ok) {
                    const result = await response.json();
                    showNotification(isEdit ? 'Site updated successfully!' : 'Site created successfully!', 'success');

                    // Redirect to sites list after a short delay
                    setTimeout(() => {
                        window.location.href = '/applications/cms/sites';
                    }, 1500);
                } else {
                    const error = await response.json();
                    showNotification(error.error || 'Failed to save site', 'error');
                }
            } catch (error) {
                console.error('Error saving site:', error);
                showNotification('Failed to save site', 'error');
            }
        }

        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            notification.className = `notification notification-${type}`;
            notification.textContent = message;
            document.body.appendChild(notification);

            setTimeout(() => {
                notification.remove();
            }, 5000);
        }

        async function logout() {
            try {
                const response = await fetch('/applications/cms/api/auth/logout', {
                    method: 'POST'
                });

                if (response.ok) {
                    window.location.href = '/applications/cms/login';
                }
            } catch (error) {
                console.error('Logout failed:', error);
            }
        }
    </script>

    <style>
        .form-container {
            max-width: 800px;
            margin: 0 auto;
        }

        .site-form {
            background: white;
            border-radius: 0.75rem;
            border: 1px solid #e2e8f0;
            overflow: hidden;
        }

        .form-section {
            padding: 2rem;
            border-bottom: 1px solid #f1f5f9;
        }

        .form-section:last-of-type {
            border-bottom: none;
        }

        .form-section h2 {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1e293b;
            margin-bottom: 1.5rem;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: #374151;
            margin-bottom: 0.5rem;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .form-group small {
            display: block;
            font-size: 0.75rem;
            color: #64748b;
            margin-top: 0.25rem;
        }

        .form-actions {
            padding: 2rem;
            background: #f8fafc;
            display: flex;
            justify-content: flex-end;
            gap: 1rem;
        }

        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
            }

            .form-section {
                padding: 1.5rem;
            }

            .form-actions {
                padding: 1.5rem;
                flex-direction: column;
            }
        }
    </style>
</body>
</html>